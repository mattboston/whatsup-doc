{
  "name": "whatsup-doc",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "build": "mkdir -p public && cp frontend/index.html public/index.html && bun build frontend/main.tsx --outdir public --target browser --minify",
    "start": "bun src/server.ts"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "bun-types": "latest"
  }
}
